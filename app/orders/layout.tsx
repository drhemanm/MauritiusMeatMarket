/**
 * Orders Layout
 * 
 * Uses the main dashboard layout
 * 
 * @module app/orders/layout
 */

export default function OrdersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
